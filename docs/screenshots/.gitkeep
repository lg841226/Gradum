# Drop your screenshots here. Required files:

# 01-hero-ide-chat.png — Hero shot: IDE + Project + Editor + Chat + Git Audit
# 02-chat-detail.png    — Chat panel rich rendering (LaTeX / tables / code / task lists)
# 03-workflow-run.png   — AWF workflow running (after Step 1 ships)
# 04-settings.png       — Settings dialog showing provider config + connection badges

# See the conversation for exact composition guidance and the
# chat conversation script that should be displayed in 01.
