# Gradle build outputs
build/
.gradle/
.kotlin/
gradle-app.setting
!gradle-wrapper.jar

# IntelliJ Platform gradle plugin local cache (layoutIndex, localPlatformArtifacts, sandbox, coroutines javaagent)
.intellijPlatform/

# Manually vendored Jewel / Compose / Skiko jars under plugin/libs/ (resolved via files("libs/...") in build.gradle.kts)
plugin/libs/

# IDE / editor files
.idea/
*.iml
*.iws
*.ipr
out/
.vscode/
*.swp
*.swo
*~

# Runtime / local data
output/
temp/
logs/
docs/idea-develop
*.log
*.jsonl
.gradum/

# OS junk
.DS_Store
Thumbs.db
desktop.ini

# Testing / temporary
test.txt
*.tmp
*.bak

# Local scratch (e.g. the Jewel README used as reference while working on the plugin)
main.txt

# Internal DSL spec + roadmap for the un-shipped AWF feature; not shown to contributors
docs/AWF_ROADMAP.md

# Secrets / sensitive config
*.secret
*.key
*.pem
gradum.properties
/gradle.properties

# Sensitive words (user-defined, do not commit)
red_line_keywords.txt
scripts/__pycache__/
**/__pycache__/
git_stats_*.csv
quality_report_*/
quality_report_*.zip
src/main/resources/red_line_keywords.txt
