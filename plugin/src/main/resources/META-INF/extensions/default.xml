<!--
  ~ Copyright (c) 2026 Gradum team, some rights reserved.
  ~ For licensing terms and conditions, see the MIT LICENSE file.
  ~
  ~ default.xml — registration manifest for the wildcard
  ~ tool-call renderer. See `ran.xml` for the manifest format.
  -->

<renderer alias="*"
          class="gradum.idea.chat.ui.chat.skill.default.DefaultRenderer"
          labelKey=""
          folder="gradum/idea/chat/ui/chat/skill/default/">
    <description>
        Wildcard default fallback for any tool-call alias that
        has no dedicated `ToolCallRenderer` extension registered.
        Registered with alias `"*"` so the registry always has
        a renderer to fall back to. Renders a minimal `Icon +
        alias` capsule with no trailing content. Third-party
        plugins should never need to register this themselves;
        the Gradum plugin ships it pre-installed.
    </description>
    <depends-on skill="(any)"/>
    <action-types/>
</renderer>
