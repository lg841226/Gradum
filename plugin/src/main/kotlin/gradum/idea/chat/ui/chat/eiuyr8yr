p1: 编辑技能工具显示 diff 行数预览，+Y 在浅色主题阅读不佳
p2: 停止按钮和其他输入框操作按钮间距可能不一致
p3: 工具调用时错误的显示不清晰，用户难以得知因为何原因而出错，需优化错误显示，如 Popup Menu 的不可选的菜单。
-> 注意点：错误提示不能过于技术化，通过查询错误码来匹配原因
-> 布局：【icon】XXXXX 出现意外错误，这不是你的原因。点击 *link(此处)* 复制详细信息。
a1: 启动插件时需将服务器 JAR 拖到对应项目中才能正常使用的体验问题。
a2: 插件端不能实时监听服务器模型状态而导致的手动刷新的体验性问题。(已完成)
-> 解决方案：启动线程专门监听状态，不阻塞 UI 主线程。

┌──────────────────────────────────────────────────────┐
│ ⚠  模型服务连接异常，这不是你的原因。                      │
│    点击此处复制详细信息                                 │
└──────────────────────────────────────────────────────┘

我的想法，整体颜色跟随 IDE 的 normal 文字，只有使用 Link 链接时为蓝色 (你也不用改) 默认就是蓝色。这个错误弹窗
应该在这个错误的工具调用矩形边框上显示。图标采用 '!' AlliconsKey.Status.Error。
